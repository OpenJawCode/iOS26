# launcher/baseline-prof

Baseline profile scaffold — **wired in Phase 8** (androidx.baselineprofile; requires androidx, which the build opts out of until Compose lands in Phase 2). Empty module keeping the graph shape stable.
