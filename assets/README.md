# assets/

Original artwork sources + generation pipeline (ADR-0012). **No Apple IP — ever.** Subdirs: `icons/` (squircle icon set sources), `wallpapers/`, `sounds/` (original UI sounds). The pipeline produces packaged asset packs consumed by `libs/icons` / companion. Personal imports never touch this directory.

Owner: design-owner. Every asset reviewable: sources, licenses (permissive), generation scripts committed alongside.
