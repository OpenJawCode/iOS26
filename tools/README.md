# tools/

Dev tooling. `codegen/` — schema → Kotlin/TS generation (owned by config-owner). `scripts/` — mirrors CI commands locally (docs/ci.md: every CI job reproducible here); dev utilities (provision, flash, snapshot, perf capture).

Owner: qa-owner (codegen: config-owner).
