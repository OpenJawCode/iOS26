# benchmarks/macrobenchmark

Macrobenchmark scaffold (androidx.benchmark) — **wired in Phase 8** with the performance budgets (ROADMAP Phase 8, ADR-0003 perf discipline). Currently an empty library so the module graph compiles from day one.

Purpose: frame-cadence, cold-start, and blur-budget baselines against `launcher/app` on the reference device; regressions fail CI.
