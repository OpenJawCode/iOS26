# device-tests/

Tier 3 harness (ADR-0013): instrumented tests + idempotent flash/verify scripts with PASS/FAIL output, version-locked manifest (device, firmware, Magisk, LSPosed). Runner-ready for the Phase 9 self-hosted CI runner (`edge20`). Every hook/module/overlay ships its verification here.

Owner: qa-owner.
