# hooks/libxposed-api

Vendored **modern libxposed API** (`io.github.libxposed.api`, Apache-2.0) — the fork-supported module API (ADR-0032). **compileOnly only**: the framework provides these classes at runtime; packaging them into a module APK is rejected ("The Xposed API classes are compiled into the module APK").
